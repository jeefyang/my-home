import { useThemeVars } from "naive-ui";

export const UserOptionDisplay: { [x in keyof UserOptionType]: { key: keyof UserOptionType, label?: string, placeholder?: string; }; } = {


};

export function dipatchUserOption(option: UserOptionType) {
    
}