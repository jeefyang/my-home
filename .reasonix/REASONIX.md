# Reasonix Code - 项目记忆

此目录存放 AI 助手的项目级记忆，提交到 Git 后可跨机器共享对话上下文。

## 如何流转

```bash
git add .reasonix/
git commit -m "更新 AI 记忆"
git push
# 换机子：
git pull
reasonix code   # 自动加载 .reasonix/ 下的记忆
```

## 当前记忆

- `memories/bookmark-feature.md` — 书签工具完成状态
